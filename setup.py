from setuptools import setup, find_packages

setup(
    name="platogram",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List any specific dependencies for platogram here
    ],
)